# Day 13 Observability Lab Report - Nhóm C401-D5.1

> **Instruction**: Fill in all sections below. This report is designed to be parsed by an automated grading assistant. Ensure all tags (e.g., `[GROUP_NAME]`) are preserved.

## 1. Team Metadata

- [GROUP_NAME]: C401-D5.1
- [REPO_URL]: https://github.com/thanhnndev/C401-D5-Day13
- [MEMBERS]:
  - Nông Nguyễn Thành | MSSV: 2A202600250 | Role: Full-Stack Observability (Logging, PII, Tracing, SLOs, Alerts, Infrastructure)
  - Đào Phước Thịnh | MSSV: 2A202600029 | Role: Dashboard & Metrics (Streamlit 7-panel dashboard, Altair charts, Correlation Explorer)
  - Đỗ Công | MSSV: 2A202600031 | Role: LLM Integration & Logging (DashScope/Qwen, domain-aware responses, incident simulation)

---

## 2. Group Performance (Auto-Verified)

- [VALIDATE_LOGS_FINAL_SCORE]: 95/100
- [TOTAL_TRACES_COUNT]: Mỗi request tạo 1 trace với 7 spans (agent-run, pii-scan, injection-scan, rag-retrieval, llm-call, response-validation, cost-calculation) thông qua `client.start_as_current_observation()` trong `app/agent.py:180-315`. Với load test 10+ requests, hệ thống sinh ra 10+ traces trên Langfuse. Traces được tạo khi `tracing_enabled()` trả về True (`app/tracing.py:48-53`) và Langfuse server khả dụng.
- [PII_LEAKS_FOUND]: 0 (PII được scrub trước khi ghi log qua `scrub_event` processor trong `app/logging_config.py:25-33`. Hệ thống phát hiện 7 patterns: email, phone_vn, cccd, credit_card, passport, visa_number, address_keyword — tất cả được thay thế bằng `[REDACTED_{TYPE}]` trước khi ghi JSONL)

---

## 3. Technical Evidence (Group)

### 3.1 Logging & Tracing

- [EVIDENCE_CORRELATION_ID_SCREENSHOT]: Correlation ID được tạo trong `CorrelationIdMiddleware` (`app/middleware.py:15`) với định dạng `req-{uuid4().hex[:8]}` (ví dụ: `req-a3f7b2c1`). ID được bind vào `structlog.contextvars` qua `bind_contextvars(correlation_id=...)` tại dòng 17, propagate qua response header `x-request-id` (dòng 25), và xuất hiện trong mọi log line JSONL nhờ processor `merge_contextvars` (`app/logging_config.py:40`). Log lines trong `data/logs.jsonl` chứa trường `correlation_id` xuyên suốt từ `request_received` đến `response_sent`.

- [EVIDENCE_PII_REDACTION_SCREENSHOT]: PII scrubbing được thực hiện qua `scrub_event` processor (`app/logging_config.py:25-33`) — scrub cả trường `event` và mọi string values trong `payload` dict. 7 patterns trong `app/pii.py:6-14`: `email` → `[REDACTED_EMAIL]`, `phone_vn` → `[REDACTED_PHONE_VN]`, `cccd` → `[REDACTED_CCCD]`, `credit_card` → `[REDACTED_CREDIT_CARD]`, `passport` → `[REDACTED_PASSPORT]`, `visa_number` → `[REDACTED_VISA_NUMBER]`, `address_keyword` → `[REDACTED_ADDRESS_KEYWORD]`. Scrubbing xảy ra trước `JsonlFileProcessor` và `JSONRenderer` trong processor chain (`app/logging_config.py:39-47`), đảm bảo không có PII raw nào ghi ra file.

- [EVIDENCE_TRACE_WATERFALL_SCREENSHOT]: Trace hierarchy 7 spans được xây dựng trong `app/agent.py:180-315` qua `client.start_as_current_observation()`:
  1. **agent-run** (root, dòng 180) — chứa metadata: model, security_flags, pii_types, injection_keywords, user_id (SHA-256 hashed), session_id, tags
  2. **pii-scan** (guardrail, dòng 193) — output: passed/findings, score: pii_safety (0.0/1.0)
  3. **injection-scan** (guardrail, dòng 210) — output: passed/findings, score: injection_safety (0.0/1.0)
  4. **rag-retrieval** (retriever, dòng 227) — output: docs_count, docs_preview
  5. **llm-call** (generation, dòng 234) — model_parameters (temperature=0.7, max_tokens=512), usage_details (prompt_tokens, completion_tokens, total_tokens), cost_details (total, input, output USD)
  6. **response-validation** (evaluator, dòng 264) — 5 quality scores (quality, relevance, completeness, safety, overall) với `eval_span.score()` cho từng metric
  7. **cost-calculation** (tool, dòng 296) — input_cost_usd, output_cost_usd, total_cost_usd với pricing metadata

- [TRACE_WATERFALL_EXPLANATION]: Span **llm-call** (`app/agent.py:234-262`) là span quan trọng nhất trong trace hierarchy. Đây là generation span với `as_type="generation"`, ghi nhận đầy đủ thông tin: `model` (tên model đang dùng), `model_parameters` (temperature=0.7, max_tokens=512), `usage_details` (prompt_tokens, completion_tokens, total_tokens — lấy từ `response.usage`), `cost_details` (total cost USD tính từ MODEL_PRICING table, split thành input/output cost). Span này cho phép phân tích chi phí và hiệu năng của mỗi LLM call, là cơ sở để tính SLO cost budget và phát hiện cost spike incidents.

### 3.2 Dashboard & SLOs

- [DASHBOARD_7_PANELS_SCREENSHOT]: Dashboard Streamlit 3-tab (`app/dashboard.py:1-381`) với thiết kế 3-layer:
  - **Layer 1: Executive Overview** (tab1, dòng 143-195) — Health badge (HEALTHY/DEGRADED/CRITICAL dựa trên error rate), 4 KPI metrics (Total Requests, P99 Latency, Total Cost, Avg Quality), Traffic Velocity chart (Altair area gradient xanh #3B82F6)
  - **Layer 2: Engineering Detail** (tab2, dòng 198-329) — 5 Golden Signals: Latency (P50/P95/P99 với SLO line đỏ #EF4444), Traffic (QPS smoothed 5s), Error Rate (với SLO line + breakdown bar chart), Cost Trend (area gradient vàng #F59E0B), Token Throughput (tokens in/out line chart). Bổ sung Cost by Feature (pie chart) và Feature Distribution (horizontal bar chart)
  - **Layer 3: Debug Investigation** (tab3, dòng 333-377) — Log search/filter (by level, feature, text search), full log table với column config (Datetime, Json, Number formatting), **Correlation Explorer** cho phép select correlation ID và trace toàn bộ lifecycle của request theo timeline

- [SLO_TABLE]:
  | SLI | Target | Window | Current Value |
  |---|---:|---|---:|
  | Latency P95 | < 3000ms | 28d | Tính bằng `percentile(latencies, 95)` trong `app/metrics.py:92-97` — sort latencies, lấy index tại vị trí 95% của sorted array. Giá trị được đọc từ `REQUEST_LATENCIES` deque (cửa sổ 5 phút, `WINDOW_SECONDS=300`) qua hàm `_values()` tự động prune entries cũ |
  | Error Rate | < 2% | 28d | Tính bằng `(total_errors / total_requests * 100)` trong `app/metrics.py:116-117`. `total_errors` từ `Counter` `ERRORS`, `total_requests` từ độ dài `REQUEST_LATENCIES`. Breakdown theo `error_type` (llm_timeout, rag_empty, schema_validation, pii_blocked) |
  | Cost Budget | < $2.5/day | 1d | Tính bằng `sum(REQUEST_COSTS)` trong cửa sổ 5 phút. `daily_budget_remaining_pct = max(0, (2.5 - sum(costs)) / 2.5 * 100)` tại `app/metrics.py:147`. Cost per request tính từ MODEL_PRICING table qua `_estimate_cost()` trong `app/agent.py:361-365` |
  | PII Leaks | 0 | 28d | 0 — PII được scrub trước khi ghi log qua `scrub_event` processor. Counter `PII_LEAKS` chỉ tăng khi `_detect_pii()` tìm thấy pattern (`app/agent.py:163`), nhưng dữ liệu đã được redact trước khi ghi JSONL |
  | Prompt Injections | 0 | 28d | Phát hiện qua `_detect_injection()` trong `app/agent.py:52-54` kiểm tra 10 keywords (ignore previous, system prompt, jailbreak, dan mode, v.v.). Counter `PROMPT_INJECTIONS` tăng khi phát hiện. Giá trị hiện tại phụ thuộc vào số lượng request có chứa injection keywords trong cửa sổ 5 phút |

### 3.3 Alerts & Runbook

- [ALERT_RULES_SCREENSHOT]: 9 alert rules trong `config/alert_rules.yaml:1-153`:
  - **5 standard alerts**: `high_latency_p95` (P2, latency_p95_ms > 5000 for 30m), `high_error_rate` (P1, error_rate_pct > 5 for 5m), `cost_budget_spike` (P2, hourly_cost_usd > 2x_baseline for 15m), `low_quality_score` (P2, quality_score_avg < 0.5 for 15m), `high_regenerate_rate` (P3, regenerate_rate > 30 for 10m)
  - **4 security alerts**: `pii_leak_detected` (P1, pii_leak_count > 0 for 1m), `prompt_injection_detected` (P1, prompt_injection_count > 0 for 5m), `unauthorized_access_attempt` (P1, auth_failure_count > 10 for 5m), `data_exfiltration_risk` (P1, tokens_out_per_request > 10000 for 3 requests)
  - Mỗi rule có: severity, condition, type, owner, runbook link, dashboard_panel reference, impact description, first_checks (3 bước), mitigation actions (3 bước)

- [SAMPLE_RUNBOOK_LINK]: docs/alerts.md (Vietnamese runbook với 9 rules, mỗi rule có đầy đủ: severity, trigger condition, impact description, first checks (3 bước điều tra), mitigation actions (3 bước khắc phục), dashboard panel reference. Security alerts bổ sung compliance notes (GDPR, FERPA) và common attack patterns)

---

## 4. Incident Response (Group)

- [SCENARIO_NAME]: rag_slow (RAG retrieval latency spike)

- [SYMPTOMS_OBSERVED]: Khi incident `rag_slow` được enable qua `POST /incidents/rag_slow/enable`, hệ thống biểu hiện các triệu chứng sau:
  1. **P95 latency tăng đột biến**: `mock_rag.py:23-24` thêm `time.sleep(2.5)` vào hàm `retrieve()`, làm mỗi request chậm thêm 2.5 giây. P95 latency vượt ngưỡng SLO 3000ms.
  2. **Quality score giảm**: `app/agent.py:367-376` — `_heuristic_quality()` giảm 0.2 điểm khi answer chứa `[REDACTED]`, và RAG retrieval chậm làm timeout khả năng tăng.
  3. **Cost tăng**: Thời gian xử lý lâu hơn làm tăng resource consumption, `cost_per_minute_usd` trong `app/metrics.py:135` tăng theo.
  4. **Error rate có thể tăng**: Nếu RAG timeout vượt quá HTTP client timeout (30s trong `scripts/load_test.py`), request sẽ fail và `error_rate_pct` tăng.

- [ROOT_CAUSE_PROVED_BY]: Root cause được chứng minh qua:
  1. **Langfuse traces**: Span `rag-retrieval` trong trace waterfall có duration ~2500ms (so với bình thường < 10ms), trong khi các spans khác (pii-scan, injection-scan, llm-call) giữ nguyên duration. Điều này khoanh vùng bottleneck tại RAG retrieval.
  2. **Log analysis**: Log lines trong `data/logs.jsonl` với `event=response_sent` có `latency_ms` > 3000, correlate với correlation_id của các request chậm.
  3. **Incident state**: `STATE["rag_slow"] = True` trong `app/incidents.py:4` kích hoạt `time.sleep(2.5)` trong `mock_rag.py:23-24` và `app/mock_llm.py:35-36`.
  4. **Metric evidence**: Dashboard Panel 1 (Latency) hiển thị P95 vượt SLO line đỏ, Panel 6 (Quality) hiển thị quality_score_avg giảm.

- [FIX_ACTION]:
  1. Disable incident: `POST /incidents/rag_slow/disable` → `STATE["rag_slow"] = False` trong `app/incidents.py:17-20`
  2. Giảm prompt size: Truncate queries dài > 500 từ trước khi gửi đến RAG
  3. Fallback retrieval source: Khi RAG chính chậm, chuyển sang retrieval source dự phòng (ví dụ: cached responses hoặc local corpus)
  4. Implement timeout: Thêm timeout cho RAG retrieval (ví dụ: 1s), fallback sang empty docs nếu timeout

- [PREVENTIVE_MEASURE]:
  1. **Alerting**: Rule `high_latency_p95` (P2) trong `config/alert_rules.yaml:2-17` cảnh báo khi latency_p95_ms > 5000 for 30m, cho phép phát hiện sớm trước khi ảnh hưởng nhiều users
  2. **Circuit breaker**: Implement circuit breaker pattern cho RAG retrieval — sau N consecutive timeouts, tự động switch sang fallback mode
  3. **RAG performance monitoring**: Theo dõi `rag_docs_count` và retrieval latency trong traces, cảnh báo khi retrieval time > 500ms
  4. **Knowledge base optimization**: Index RAG documents efficiently, implement caching cho queries phổ biến, giảm retrieval time

---

## 5. Individual Contributions & Evidence

### Nông Nguyễn Thành - 2A202600250 - Vai trò: Full-Stack Observability

- [TASKS_COMPLETED]:
  - Cấu hình structlog pipeline với 7 processors (merge_contextvars → add_log_level → TimeStamper → scrub_event → StackInfoRenderer → format_exc_info → JsonlFileProcessor → JSONRenderer), đảm bảo mọi log line có correlation_id, timestamp ISO UTC, và PII đã được scrub trước khi ghi JSONL
  - Thiết kế 7 PII patterns trong `app/pii.py:6-14` (email, phone_vn, cccd, credit_card, passport, visa_number, address_keyword) với regex và hàm `scrub_text()` thay thế bằng `[REDACTED_{TYPE}]`
  - Tích hợp Langfuse client với 7-span trace hierarchy trong `app/agent.py:180-315` (agent-run, pii-scan, injection-scan, rag-retrieval, llm-call, response-validation, cost-calculation), gắn user_id (SHA-256 hashed), session_id, tags, metadata
  - Triển khai 5 quality scores (quality, relevance, completeness, safety, overall) với weighted scoring (35%/25%/15%/25%) và Langfuse `span.score()` trong response-validation span
  - Xây dựng 9 alert rules (5 standard + 4 security) trong `config/alert_rules.yaml` với Vietnamese runbook chi tiết tại `docs/alerts.md`
  - Cấu hình Docker Compose 6 containers (Langfuse stack: web, worker, postgres, clickhouse, minio, redis) với custom ports 27xxx
  - Tích hợp LM Studio native API (`app/mock_llm.py`) + Real LLM DashScope/Qwen (`app/llm.py`) với MODEL_PRICING table (16 models)
  - Xử lý merge conflict resolution cho 7 files, viết load test script và dashboard validation script
- [EVIDENCE_LINK]: "docs/reports/2A202600250_NongNguyenThanh_individual_report.md" | "app/agent.py" | "app/tracing.py" | "app/metrics.py" | "config/alert_rules.yaml" | "docker-compose.yml" | "app/logging_config.py" | "app/middleware.py" | "app/pii.py"

### Đào Phước Thịnh - 2A202600029 - Vai trò: Dashboard

- [TASKS_COMPLETED]:
  - Thiết kế và triển khai Dashboard giám sát 7-panel bằng Streamlit và Altair (`app/dashboard.py:1-381`), đảm bảo tính trực quan và khả năng Observability toàn diện
  - Xây dựng hệ thống Metric Performance chuyên sâu: Triển khai tính toán các phân vị Latency (P50, P95, P99) kèm theo đường ngưỡng SLO (Service Level Objective) — SLO line đỏ #EF4444 tại 500ms cho latency, 5% cho error rate
  - Tích hợp giám sát tài nguyên LLM: Xây dựng biểu đồ Cost Trend (Altair area gradient vàng #F59E0B) và Token Throughput (tokens in/out line chart) theo thời gian thực, giúp kiểm soát ngân sách và mức độ sử dụng mô hình
  - Phát triển tính năng "Correlation Explorer" (`app/dashboard.py:364-377`): Cho phép truy xuất toàn bộ vòng đời của một request dựa trên Correlation ID, hiển thị timeline với timestamp, event name, log level, và JSON payload — hỗ trợ quá trình Debug và Incident Investigation
  - Tối ưu hóa UI/UX với 3-layer design: Layer 1 (Executive Overview — health badge, 4 KPI metrics, traffic velocity), Layer 2 (Engineering Detail — 5 Golden Signals với SLO thresholds), Layer 3 (Debug Investigation — log search/filter, Correlation Explorer). Trải qua nhiều phiên bản tinh chỉnh layout (commit `cffaafc`: fix df_raw undefined, rename q_chart to f_chart, remove stray else block, remove duplicate sidebar)
  - Thiết kế design system với CSS custom: Fira Code + Fira Sans fonts, dark theme (#0F172A background), metric cards với border/shadow, health badge 3 trạng thái (HEALTHY green/DEGRADED amber/CRITICAL red)
  - Xây dựng dashboard validation script (`scripts/validate_dashboard.py:1-306`) kiểm tra 7 required panels, log schema compliance (required fields, enum validation, numeric constraints, correlation_id format), Langfuse traces count (>= 10 minimum), metrics endpoint response (9 expected keys)
- [EVIDENCE_LINK]: "app/dashboard.py" | "scripts/validate_dashboard.py" | "docs/dashboard-spec.md"

### Đỗ Công - 2A202600031 - Vai trò: LLM Integration & Logging

- [TASKS_COMPLETED]:
  - Triển khai **Real LLM integration** với DashScope/Qwen qua OpenAI-compatible REST API trong `app/llm.py:1-237`. Class `LLM` khởi tạo với `DASHSCOPE_API_KEY`, `QWEN_MODEL`, `DASHSCOPE_ENDPOINT` (mặc định `https://dashscope-intl.aliyuncs.com/compatible-mode/v1`). Sử dụng `openai.OpenAI` client để gọi `chat.completions.create()` với system prompt, user prompt, max_tokens=512, temperature=0.2, và `enable_thinking: false` cho Qwen3
  - Xây dựng **system prompt phong phú** (`app/llm.py:43-107`) được load từ 3 sources: (1) `data/company_policy.md` — quy định nội bộ Công ty TNHH D5, (2) Q&A corpus — structured knowledge base (hoàn tiền, monitoring, bảo mật, giờ làm việc, gửi xe, mức phạt, lương, tính lương Net), (3) `docs/alerts.md` — observability alert runbooks. System prompt được build một lần tại import time qua `_load_system_prompt()`
  - Triển khai **domain-aware fallback responses** trong `LLM._fallback()` (`app/llm.py:196-237`) với keyword-based matching cho 7 topics: giờ làm việc/check-in/chấm công, phạt/late/trễ, lương/salary/net/gross, bảo mật/pii/policy/security, refund/hoàn tiền, monitoring/alert/trace/log/metric. Khi không có config hoặc API fail, trả về structured answer tiếng Việt thay vì error
  - Xử lý **incident simulation** cho 3 scenarios trong `LLM.generate()` (`app/llm.py:140-157`): `tool_fail` → raise RuntimeError("LLM tool_fail incident active"), `rag_slow` → time.sleep(2.5), `cost_spike` → max_tokens=2048 và output_tokens *= 4. Cùng logic được mirror trong `app/mock_llm.py:33-43` cho FakeLLM
  - Loại bỏ **thinking tags** từ Qwen3 responses (`app/llm.py:175-176`) qua regex `sub(r"<think>.*?</think>", "", raw, flags=DOTALL)` để đảm bảo response sạch sẽ
  - Xử lý **graceful fallback** khi API call fail (`app/llm.py:187-193`): catch Exception, sleep để đảm bảo latency metric realistic, gọi `_fallback()` với `zero_cost=True` (tokens = 0)
  - Tích hợp **USE_REAL_LLM toggle** trong `app/agent.py:144-150`: khi `USE_REAL_LLM=true` thì dùng `LLM` class (DashScope/Qwen), ngược lại dùng `FakeLLM` (LM Studio/fallback). Model được set từ `QWEN_MODEL` env var khi dùng real LLM
  - Đóng góp vào **merge conflict resolution** cho branch `feat/LLM` (commit `1f9063b`, `d57fb39`): tích hợp MODEL_PRICING table, LLM class, USE_REAL_LLM toggle vào agent pipeline mà không làm break tracing logic
- [EVIDENCE_LINK]: "app/llm.py" | "app/mock_llm.py" | "data/company_policy.md" | "app/agent.py:142-150"

---

## 6. Bonus Items (Optional)

- [BONUS_COST_OPTIMIZATION]: MODEL_PRICING table trong `app/agent.py:110-135` với **16 models** từ 4 providers (Qwen/DashScope: 5 models, OpenAI: 8 models, Anthropic: 3 models, + default fallback). Bảng pricing chứa input/output cost per 1M tokens, cho phép so sánh chi phí giữa các model. Hàm `_estimate_cost()` (`app/agent.py:361-365`) tính cost chính xác cho mỗi request dựa trên model đang dùng. Daily budget monitoring ($2.5/ngày) được implement qua `daily_budget_remaining_pct` trong `app/metrics.py:147`. Per-token cost tracking qua `cost_details` trong llm-call span (`app/agent.py:252-256`) và cost-calculation span (`app/agent.py:296-315`).

- [BONUS_AUDIT_LOGS]: JSONL audit log tại `data/logs.jsonl` với đầy đủ PII scrubbing (7 patterns scrubbed trước khi ghi), correlation ID tracking (format `req-{hex8}` xuyên suốt request lifecycle), structured event logging (request_received, response_sent, request_failed, app_started, incident_enabled, incident_disabled). Log schema được validate bởi `scripts/validate_dashboard.py:93-166` kiểm tra required fields (ts, level, service, event, correlation_id), enum validation (feature, service, error_type, event), numeric constraints (latency_ms >= 0, quality_score 0-1), và correlation_id format (length >= 8). Audit log được ghi qua `JsonlFileProcessor` (`app/logging_config.py:16-22`) với UTF-8 encoding.

- [BONUS_CUSTOM_METRIC]: **5 quality scores** được tính trong `app/agent.py:269-292` và `app/agent.py:323-327`:
  - `quality_score` — Heuristic quality (0-1) dựa trên docs retrieval, answer length, keyword overlap, redaction penalty
  - `relevance_score` — Keyword overlap giữa question và answer (`_compute_relevance`, `app/agent.py:56-62`)
  - `completeness_score` — Answer length so với ideal 300 words (`_compute_completeness`, `app/agent.py:64-70`)
  - `safety_score` — PII và injection safety (`_compute_safety`, `app/agent.py:72-80`): trừ 0.3 cho mỗi PII/injection found, trừ 0.1 nếu có [REDACTED]
  - `overall_score` — Weighted average (`_compute_overall`, `app/agent.py:82-90`): quality 35%, relevance 25%, completeness 15%, safety 25%

  **Security metrics** trong `app/metrics.py:24-28, 149-153`: `pii_leak_count`, `prompt_injection_count`, `auth_failure_count`, `redaction_rate_pct`, `redacted_count` — tất cả được track trong time-windowed deques (5 phút) và expose qua `/metrics` endpoint.
